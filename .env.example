# Frontend (Vite) — copy to .env.local in repo root

VITE_API_BASE_URL=http://localhost:4000/api
VITE_WS_URL=ws://localhost:4000/api/ws/quotes
VITE_RAZORPAY_KEY_ID=

# Backend env vars → see backend/.env.example
